function setup() {
  createCanvas(1000,600);
}

function draw() {
 
  fill(0);
  ellipse(mouseX, mouseY, 100, 100);
  noStroke();
  
  
  textSize(30);
  fill(255);
  text("Back in 6th Grade, I learned about how being a game designer was", 60, 240);
  text("a feasable career choice. I was convinced with the idea immediately. ", 60, 300);
  text("From then on, It became a goal that I still pursue to this day.", 60, 360);

  
  
}